import { Text } from "@chakra-ui/react";

import { ContentLayout } from "@/components/Layout";

export const Retros = () => {
  return (
    <ContentLayout title="Retros">
      <Text>Retros placeholder</Text>
    </ContentLayout>
  );
};
